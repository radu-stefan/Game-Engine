#include "stdafx.h"
#include "LightObject.h"


LightObject::LightObject()
{
}


LightObject::~LightObject()
{
}

void LightObject::Update(ESContext * esContext, float deltaTime)
{
	/*
	Matrix rotM, rotX, rotY, rotZ;
	rotM = rotX.SetRotationX(rotation.x) * rotY.SetRotationY(rotation.y) * rotZ.SetRotationZ(rotation.z);

	direction = Vector4(0,-1,0,1.0);
	direction = rotM * direction;
	*/
}
